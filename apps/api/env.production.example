# Production Environment Variables
# Copy this to .env.production and fill in all values

NODE_ENV=production
PORT=4000
LOG_LEVEL=info

# --- Database ---
# Production PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:YOUR_STRONG_PASSWORD@your-db-host:5432/vett

# --- Redis ---
# Production Redis connection string
# Format: redis://:password@host:port or redis://host:port
REDIS_URL=redis://your-redis-host:6379

# --- Authentication ---
# Clerk.dev production secret key (starts with sk_live_)
CLERK_SECRET_KEY=sk_live_YOUR_PRODUCTION_KEY

# --- CORS ---
# Comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=https://app.yourdomain.com,https://admin.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# --- External API Keys ---
# OpenAI API key
OPENAI_API_KEY=sk-proj-YOUR_KEY

# Anthropic API key (optional)
ANTHROPIC_API_KEY=sk-ant-YOUR_KEY

# Pinecone API key (optional)
PINECONE_API_KEY=YOUR_KEY

# --- File Uploads ---
# CDN URL for serving uploaded files
# Example: PUBLIC_UPLOAD_BASE_URL=https://cdn.yourdomain.com
PUBLIC_UPLOAD_BASE_URL=https://cdn.yourdomain.com

# Upload directory (local path or S3 bucket)
# For S3: s3://bucket-name/uploads
# For local: /app/uploads
UPLOADS_DIR=/app/uploads

# --- Error Tracking (Sentry) ---
# Sentry DSN for error tracking (highly recommended for production)
# Get from https://sentry.io/settings/[org]/projects/[project]/keys/
SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
SENTRY_ENVIRONMENT=production
# Performance monitoring sample rate (0.0 to 1.0)
# 0.1 = 10% of transactions, 1.0 = 100% (use lower in production)
SENTRY_TRACES_SAMPLE_RATE=0.1

